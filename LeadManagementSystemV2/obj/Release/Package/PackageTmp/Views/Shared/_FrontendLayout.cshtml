@using LeadManagementSystemV2.Models;
<!DOCTYPE html>
<html lang="en">

<head>

    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <meta name="description" content="">
    <meta name="author" content="">
    <link rel="shortcut icon" href="~/assets/img/fav-icon.png" />
    <title>Descon Intranet</title>

    <!-- Bootstrap core CSS -->
    <link href="~/assets/vendor/bootstrap/css/bootstrap.min.css" rel="stylesheet">

    <!-- Custom styles for this template -->
    <link href="~/assets/css/custom.css" rel="stylesheet">
    <link href="~/assets/css/fontawesome.min.css" rel="stylesheet">
    <link href="~/assets/owl/owl.carousel.min.css" rel="stylesheet">
    <link href="~/assets/owl/owl.theme.default.min.css" rel="stylesheet">
    <link href="~/assets/calender/main.min.css" rel="stylesheet">
    <link href="~/assets/datatable/datatables.min.css" rel="stylesheet">
    <link href="~/assets/sidebar/style3.css" rel="stylesheet">
    <link href="~/assets/sidebar/jquery.mCustomScrollbar.min.css" rel="stylesheet">
    <!-- CSS -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/alertifyjs@1.13.1/build/css/alertify.min.css" />
    <!-- Default theme -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/alertifyjs@1.13.1/build/css/themes/default.min.css" />
    <style>
        #loading {
            width: 100%;
            height: 100%;
            top: 0px;
            left: 0px;
            position: fixed;
            display: block;
            z-index: 99;
            opacity: 0.7;
            background-color: #fff;
            display: none;
        }

        #loading-image {
            position: absolute;
            top: 20%;
            left: 40%;
            z-index: 100
        }


        .w-h1 {
            text-align: center;
        }

        .w-input {
            padding: 10px;
            width: 100%;
            font-size: 17px;
            font-family: Raleway;
            border: 1px solid #aaaaaa;
        }

        .info {
            padding: 10px;
            width: 100%;
            font-size: 17px;
            font-family: Raleway;
            border: 1px solid #aaaaaa;
        }

        /* Mark input boxes that gets an error on validation: */
        .w-input.invalid {
            background-color: #ffdddd;
        }

        /* Hide all steps by default: */
        .tab {
            display: none;
        }

           

        .w-button {
            background-color: #2C3E50;
            color: #ffffff;
            border: none;
            padding: 10px 20px;
            font-size: 17px;
            font-family: Raleway;
            cursor: pointer;
        }

            .w-button:hover {
                opacity: 0.8;
            }

        #prevBtn {
            background-color: #bbbbbb;
        }

        /* Make circles that indicate the steps of the form: */
        .step {
            height: 15px;
            width: 15px;
            margin: 0 2px;
            background-color: #bbbbbb;
            border: none;
            border-radius: 50%;
            display: inline-block;
            opacity: 0.5;
        }

            .step.active {
                opacity: 1;
            }

            /* Mark the steps that are finished and valid: */
            .step.finish {
                background-color: #04AA6D;
            }

        .notice-board ul li {
            margin-bottom: 10px;
            color: #ffffff;
            line-height: normal;
            text-overflow: ellipsis;
            overflow: hidden;
            display: -webkit-box;
            -webkit-line-clamp: 3;
            -webkit-box-orient: vertical;
            
            max-height: 57px;
        }

    </style>

</head>

<body>
    <div id="loading">
        <img id="loading-image" src="~/assets/images/loader.gif" alt="Loading..." />
    </div>


    <!-- Sidebar  -->
    <nav id="sidebar">
        <div id="dismiss">
            <i class="fas fa-arrow-right"></i>
        </div>

        <div class="sidebar-header">
            <h5 class="mb-0">Business Applications</h5>
        </div>

        <ul class="list-unstyled components">


            @if (ViewBag.BApp != null)
            {
                var businessApps = (List<BusinessApplication>)ViewBag.BApp;
                foreach (var item in businessApps.Where(x => x.IsFooterLink.Equals("BA")))
                {
                    <li> <a href="@item.Url" target="_blank">@item.Name</a></li>
                }
            }
        </ul>


    </nav>
    <!-- Navigation -->


    <div class="overlay"></div>
    <!-- Navigation -->
    <nav class="navbar  navbar-expand-md navbar-light fixed-top" id="mainNav">
        <div class="container-fluid spacer-padding">
            <a class="navbar-brand" href="@Url.Action("","home")"><img src="~/assets/img/logo.png" alt="logo" class="img-fluid logo"></a>
            <!-- <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse"
              data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false"
              aria-label="Toggle navigation">
              <span class="navbar-toggler-icon"></span>
            </button> -->
            <div>
                <ul class="navbar-nav ml-auto">
                    <li>
                        <button type="button" id="sidebarCollapse" class="btn float-right">
                            <span>Business Applications</span>
                            <i class="fas fa-align-left"></i>
                        </button>
                    </li>

                </ul>
            </div>
        </div>
    </nav>
    <!-- Navigation -->
    <!--Body-->
    @RenderBody()
    <!--Body-->
    <!-- Footer -->
    <!-- Footer -->
    <footer class="py-3 bg-dark mt-5">
        <div class="container-fluid spacer-padding">
            <div class="row mb-3">

                <!-- Grid column -->
                <div class="col-xl-4 col-lg-4 col-md-4 col-sm-4 col-12 text-sm-left text-center mb-md-1 mb-4">

                    <!-- Content -->
                    <img src="~/assets/img/logo.png" alt="logo" class="img-fluid logo mb-3">

                </div>
                <!-- Grid column -->
                <!-- Grid column -->
                <div class="col-xl-4 col-lg-4 col-md-4 col-sm-4 col-12 text-sm-left text-center  mb-md-1 mb-4">

                    <!-- Links -->
                    <h3>Social</h3>

                    <p class="m-0">
                        <a class="align-items-center d-block mb-1" href="#"><i class="fab fa-facebook-square fa-2x"></i>Facebook</a>
                        <a class="align-items-center d-block" href="#"><i class="fab fa-linkedin  fa-2x"></i>linkedin&nbsp;&nbsp;</a>
                    </p>

                </div>
                <!-- Grid column -->
                <!-- Grid column -->
                <div class="col-xl-4 col-lg-4 col-md-4 col-sm-4 col-12 text-sm-left text-center  mb-md-1 mb-0">

                    <!-- Links -->
                    <h3>Links</h3>

                    <ul class="">
                        @if (ViewBag.BApp != null)
                        {
                            var footerLinks = (List<BusinessApplication>)ViewBag.BApp;
                            foreach (var item in footerLinks.Where(x => x.IsFooterLink.Equals("Footer")))
                            {
                                <li>
                                    <a href="@item.Url">@item.Name</a>
                                </li>
                            }
                        }


                        <li>
                            <a href="@Url.Action("explorer",new {type = HttpUtility.UrlEncode("SAP") })">SAP Training Manuals</a>
                        </li>

                    </ul>

                </div>
                <!-- Grid column -->

            </div>
            <div class="row">
                <div class="col-12 my-auto">
                    <small class="m-0  text-center d-block">Descon © 2020 All Rights Reserved</small>
                </div>
            </div>
        </div>
        <!-- /.container -->
    </footer>



    <!-- JavaScript -->
    @*<script src="~/assets/vendor/jquery/jquery.min.js"></script>*@
    @Scripts.Render("~/assets/cache/jquery")
    <script src="~/assets/js/jquery.validate.js"></script>
    <script src="~/assets/js/jquery.unobtrusive-ajax.min.js"></script>
    <script src="~/assets/js/jquery.validate.unobtrusive.js"></script>
    <script src="~/assets/vendor/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="~/assets/js/script.js"></script>
    <script src="~/assets/owl/owl.carousel.min.js"></script>
    <script src="~/assets/calender/main.min.js"></script>
    <script src="~/assets/datatable/datatables.min.js"></script>
    <script src="~/assets/js/notify.js"></script>
    <script src="~/assets/sidebar/jquery.mCustomScrollbar.concat.min.js"></script>
    <script src="~/assets/js/script.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/alertifyjs@1.13.1/build/alertify.min.js"></script>
    @Scripts.Render("~/assets/cache/function")
    @if (IsSectionDefined("LinkScriptSection"))
    {
        @RenderSection("LinkScriptSection");
    }
    @Scripts.Render("~/assets/cache/function")
    <script>
        var currentTab = 0; // Current tab is set to be the first tab (0)
        showTab(currentTab); // Display the current tab

        function showTab(n) {
            // This function will display the specified tab of the form...
            var x = document.getElementsByClassName("tab");
            x[n].style.display = "block";
            //... and fix the Previous/Next buttons:
            if (n == 0) {
                document.getElementById("prevBtn").style.display = "none";
            } else {
                document.getElementById("prevBtn").style.display = "inline";
            }
            if (n == (x.length - 1)) {
                document.getElementById("nextBtn").innerHTML = "Submit";
            } else {
                document.getElementById("nextBtn").innerHTML = "Next";
            }
            //... and run a function that will display the correct step indicator:
            fixStepIndicator(n)
        }

        function nextPrev(n) {
            // This function will figure out which tab to display
            var x = document.getElementsByClassName("tab");
            // Exit the function if any field in the current tab is invalid:
            if (n == 1 && !validateForm()) return false;
            // Hide the current tab:
            x[currentTab].style.display = "none";
            // Increase or decrease the current tab by 1:
            currentTab = currentTab + n;
            // if you have reached the end of the form...
            if (currentTab >= x.length) {
                // ... the form gets submitted:
                document.getElementById("regForm").submit();
                return false;
            }
            // Otherwise, display the correct tab:
            showTab(currentTab);
        }

        function validateForm() {
            // This function deals with validation of the form fields
            var x, y, i, valid = false;
            x = document.getElementsByClassName("tab");
            y = x[currentTab].getElementsByClassName("w-input");
            var inputs = document.getElementsByClassName("w-input");
            $.each(inputs, function () {
                if ($(this).is(":checked")) {
                    valid = true;
                }
            })

            if (valid) {
                document.getElementsByClassName("step")[currentTab].className += " finish";
            }
            return valid; // return the valid status
        }

        function fixStepIndicator(n) {
            // This function removes the "active" class of all steps...
            var i, x = document.getElementsByClassName("step");
            for (i = 0; i < x.length; i++) {
                x[i].className = x[i].className.replace(" active", "");
            }
            //... and adds the "active" class on the current step:
            x[n].className += " active";
        }
    </script>
    <script>

        function GetGMT2() {
            $.ajax({
                url: "http://worldtimeapi.org/api/timezone/Etc/GMT-2",
                type: "Get",
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded'
                },
                success: function (response) {
                    let time = new Date(response.datetime);
                    $(".Time-2").html(time.toLocaleString('en-US', { timeZone: 'Etc/GMT-2', hour: '2-digit', minute: '2-digit' }));
                }
            });

        }
        function GetGMT3() {
            $.ajax({
                url: "http://worldtimeapi.org/api/timezone/Etc/GMT-3",
                type: "Get",
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded'
                },
                success: function (response) {
                    let time = new Date(response.datetime);
                    $(".Time-3").html(time.toLocaleString('en-US', { timeZone: 'Etc/GMT-3', hour: '2-digit', minute: '2-digit' }));
                }
            });
        }
        function GetGMT4() {
            $.ajax({
                url: "http://worldtimeapi.org/api/timezone/Etc/GMT-4",
                type: "Get",
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded'
                },
                success: function (response) {
                    let time = new Date(response.datetime);
                    $(".Time-4").html(time.toLocaleString('en-US', { timeZone: 'Etc/GMT-4', hour: '2-digit', minute: '2-digit' }));
                }
            });
        }
        function GetGMT5() {
            //$(".Time-4").html(time.toTimeString(undefined, {
            //        //    hour: '2-digit',
            //        //    minute: '2-digit',
            //        //}));
            $.ajax({
                url: "http://worldtimeapi.org/api/timezone/Etc/GMT-5",
                type: "Get",
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded'
                },
                success: function (response) {
                    let time = new Date(response.datetime);
                    $(".Time-5").html(time.toLocaleString('en-US', { timeZone: 'Etc/GMT-5', hour: '2-digit', minute: '2-digit' }));
                }
            });
        }
        $(document).ready(function () {
            $.ajax({
                url: "https://free.currconv.com/api/v7/convert?q=USD_PKR&compact=ultra&apiKey=c6cbd4438f3cf0d2cdf6",
                type: "Get",
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded'
                },
                dataType: "json",
                success: function (response) {
                    $(".usd").html(parseFloat(response.USD_PKR).toFixed(2));
                }
            });
            $.ajax({
                url: "https://free.currconv.com/api/v7/convert?q=QAR_PKR&compact=ultra&apiKey=c6cbd4438f3cf0d2cdf6",
                type: "Get",
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded'
                },
                dataType: "json",
                success: function (response) {
                    $(".qatar").html(parseFloat(response.QAR_PKR).toFixed(2));
                }
            });
            $.ajax({
                url: "https://free.currconv.com/api/v7/convert?q=AED_PKR&compact=ultra&apiKey=c6cbd4438f3cf0d2cdf6",
                type: "Get",
                headers: {
                    'Content-Type': 'application/x-www-form-urlencoded'
                },
                dataType: "json",
                success: function (response) {
                    $(".uae").html(parseFloat(response.AED_PKR).toFixed(2));
                }
            })
            GetGMT2();
            GetGMT3();
            GetGMT4();
            GetGMT5();

        });
    </script>



    @if (IsSectionDefined("InLineScriptSection"))
    {
        @RenderSection("InLineScriptSection");
    }
    <script>
        $(document).ready(function () {
            $(document).on('contextmenu', 'img', function () {
                return false;
            });
        });
    </script>
</body>

</html>
